import React from 'react';
import ModalPane from './modal_pane';

const Modal = (props) => {
    return (  
        <div className="modal">
            <ModalPane />
        </div>
    );
}
 
export default Modal;